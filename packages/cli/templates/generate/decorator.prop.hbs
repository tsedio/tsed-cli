import {applyDecorators} from "@tsed/core";
import {PropertyMetadata} from "@tsed/common";

export interface I{{symbolName}}Options {

}

export function {{symbolName}}(options: I{{symbolName}}Options): PropertyDecorator {
  return applyDecorators(
    propertyFn((propertyMetadata: PropertyMetadata) => {

    })
  );
}

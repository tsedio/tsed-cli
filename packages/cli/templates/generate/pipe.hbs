import {Injectable} from "@tsed/di";
import {HandlerContext, IPipe, ParamMetadata} from "@tsed/common";

@Injectable()
export class {{symbolName}} extends IPipe {
  transform(value: any, param: ParamMetadata) {
    return null;
  }
}

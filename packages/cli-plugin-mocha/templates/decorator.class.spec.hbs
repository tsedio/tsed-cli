import { expect } from "chai";
import { {{symbolName}} } from "./{{symbolName}}";

describe("{{symbolName}}", () => {
  it("should do something", () => {
    @{{symbolName}}()
    class Test {
    }

    expect({{symbolName}}).to.be.a("function")
    expect({{symbolName}}()).to.be.a("function")
  }));
});

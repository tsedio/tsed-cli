import { expect } from "chai";
import { {{symbolName}} } from "./{{symbolPathBasename}}.js";

describe("{{symbolName}}", () => {
  it("should do something", () => {
    expect(new {{symbolName}}()).to.be.instanceof({{symbolName}});
  });
});
